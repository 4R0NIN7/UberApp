package com.untitledkingdom.ueberapp.service.utils

/*
@Component.Builder
interface ScopeBuilder {
    @BindsInstance
    fun scope(scope: CoroutineScope): ScopeBuilder
}

interface HasScope {
    fun scope(): CoroutineScope
}

@InstallIn(Singleton::class)
@Module
object ScopeModule {
    @Provides
    fun provideScope(any: Any): CoroutineScope? {
        return if (any is HasScope) {
            any.scope()
        } else
            null
    }
}
*/
